[build]
  # Static site — no build step. Publish the repo root as-is.
  # A real no-op command is required so this overrides the UI's "npm run build"
  # (an empty string is treated as "unset" and Netlify falls back to the UI value).
  publish = "."
  command = "echo 'No build needed — static site'"
