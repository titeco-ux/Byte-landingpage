[build]
  # Static site, no real build. The deploy step expects a "dist" directory,
  # so assemble one by copying the static files into it.
  publish = "dist"
  command = "rm -rf dist && mkdir -p dist && cp -r index.html css js assets dist/"
